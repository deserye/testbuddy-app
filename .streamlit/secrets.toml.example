# Copy these values into Streamlit Community Cloud > App settings > Secrets.
# Never commit the real key or paste it into the application UI.
OPENAI_API_KEY = "replace-with-your-server-side-secret"
# Optional: use a different supported model from the live catalog.
# CPF_LLM_MODEL = "gpt-5-mini"
# Optional: omit when using the default proxy endpoint.
# OPENAI_API_BASE = "https://api.openai.com/v1"
