# Copy these values into Streamlit Community Cloud > App settings > Secrets.
# Never commit the real key or paste it into the application UI.
OPENAI_API_KEY = ""
CARE_LLM_MODEL = "gpt-4o-mini"

# Basic prototype roles. Replace these values before sharing or deploying.
USER_PASSWORD = "replace-with-a-user-password"
ADMIN_PASSWORD = "replace-with-an-admin-password"

# Optional whole-application password gate.
APP_PASSWORD = ""

# Optional compatible custom endpoint.
# OPENAI_API_BASE = "https://api.openai.com/v1"
